# PostgreSQL connection string
# Format: postgresql://usuario:password@host:puerto/nombre_base_de_datos
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/admin_portal

# Default company slug used for single-tenant installs (uses the 'public' schema)
DEFAULT_COMPANY_SLUG=public
